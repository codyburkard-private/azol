# azol

A python-based pentesting library for Azure and Entra ID.

Install with `pip install azol`. See the repository README for quickstart examples.

## Documentation

- [GraphClient usage](clients/graph.md) — authentication, fluent `call()`, and annotations
- [GraphClient API reference](reference/graph_client.md) — auto-generated from source docstrings

Build these docs locally:

```bash
pip install -e ".[docs]"
mkdocs serve
```
