# Kudu / App Service

[`KuduClient`](../reference/clients/kudu_client.md) talks to the App Service / Function Kudu SCM endpoint for process, environment, and VFS-style operations useful during assessments.

Authenticate with a credential that can obtain a token for the site or use site-level credentials as documented on the client.
