# Installation

```bash
pip install azol
```

Requires Python 3. On Windows, the optional Windows Authentication Manager broker dependency (`pymsalruntime`) is installed automatically when available.

After install, import from the top-level package or submodules:

```python
from azol import GraphClient, ArmClient, User, ServicePrincipal
```
